# MonikaSearch 隐私说明（v0.3.0）

一句话总结：**索引、配置、日志全部只保存在你自己的电脑上，不会上传；唯一会离开
电脑的数据，是你在 AI 搜索（`->ai`）里输入的那句话**，发往你自己配置的接口。

## 一、会离开本地的数据（仅一项）

- 触发 AI 搜索（输入以 `->ai` 结尾并回车）时，程序把**你输入的那句自然语言**
  连同一段固定的系统提示词，发送到设置里配置的 OpenAI 兼容接口
  （DeepSeek、OpenAI 等，由你自行填写并保存在设置中）。
- **不发送**：文件名、路径、索引内容、搜索结果、文件内容、系统/硬件信息。
- 普通搜索、右键菜单、缩略图、MCP 搜索全部在本地完成，不产生任何网络请求。

## 二、保存在本地的数据（`%LOCALAPPDATA%\MonikaSearch\`）

| 文件 | 内容 |
| --- | --- |
| `index.bin` | 全盘文件名索引（大小随文件总数增长，百万级文件约数百 MB）。只含 NTFS 文件名、目录结构、修改时间，**不含文件内容** |
| `config.json` | AI 接口地址、模型名、API Key（**明文**，仅在本地；请勿把该文件发给别人）。也可用环境变量 `MONIKA_AI_KEY` / `DEEPSEEK_API_KEY` 注入，不落盘 |
| `ctxmenu.log` | 右键菜单宿主的诊断日志 |
| `menu_skip.txt` | 自动诊断出的、需在本程序内屏蔽的问题扩展 CLSID |

删除该目录即可彻底清除全部本地数据。

## 三、权限与系统行为

- 程序要求**管理员权限**：仅用于读取 NTFS 的 MFT / USN Journal（常规 API 无法
  枚举全盘文件）。不做任何系统设置修改、不写注册表、不装驱动、不装服务。
- “删除（移到回收站）”调用系统 `SHFileOperation`（FOF_ALLOWUNDO），进回收站，可还原。
- 右键菜单交给 Windows 原生 shell 菜单执行；为隔离第三方扩展的崩溃，菜单在独立
  进程 `ctxhost.exe` 中弹出。“卡死扩展自动诊断”只**读取**注册表里已登记的处理程序
  清单，屏蔽只在 MonikaSearch 自己的进程内生效，不影响资源管理器。

## 四、无遥测

程序不含任何统计上报、崩溃上报、自动更新或后台联网组件。全部网络行为就是
第一节那一处，且只在 你主动触发 AI 搜索 时发生。
